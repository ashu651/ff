{
  "name": "snapzy",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*/*"
  ],
  "scripts": {
    "dev": "pnpm -C apps/api start:dev & pnpm -C apps/web dev",
    "build": "pnpm -C apps/api build && pnpm -C apps/web build",
    "test": "pnpm -C apps/api test && pnpm -C apps/web test",
    "migrate": "pnpm -C apps/api prisma:migrate",
    "seed": "pnpm -C apps/api seed"
  },
  "engines": { "node": ">=20" }
}