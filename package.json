{
  "name": "snapzy",
  "private": true,
  "version": "1.0.0",
  "description": "Snapzy - Instagram-like full-stack app (monorepo)",
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "dev": "npm run -w apps/backend dev & npm run -w apps/frontend dev",
    "build": "npm run -w apps/backend build && npm run -w apps/frontend build",
    "start": "npm run -w apps/backend start & npm run -w apps/frontend start",
    "lint": "npm run -w apps/frontend lint"
  },
  "engines": {
    "node": ">=18.17.0",
    "npm": ">=9.0.0"
  }
}